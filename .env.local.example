# Local Environment Configuration (copy to .env.local to override)
# This file is for local development overrides only. Do not commit .env.local

# Backend Server Configuration
PORT=3000
HOST=localhost
PUBLIC_HOST=localhost
WS_PATH=/stomp

# Optional: Override API base URL for debugging
# API_BASE_URL=http://localhost:8080

# Optional: Set NODE_ENV for development logging
# NODE_ENV=development
